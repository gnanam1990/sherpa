{
  "name": "@sherpa/core",
  "version": "0.0.0",
  "private": true,
  "description": "Agent core: executor, planner, parser. (owner: M1)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@sherpa/identity": "workspace:*",
    "@sherpa/llm": "workspace:*",
    "@sherpa/memory": "workspace:*",
    "@sherpa/safety": "workspace:*",
    "@sherpa/tools": "workspace:*",
    "viem": "^2.50.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
