/**
 * @sherpa/tools — Protocol adapters (M1 ownership).
 *
 * Every adapter exports `quote()`, `buildTx()`, `verify()`. Naming is
 * non-negotiable per M1_BACKEND_PACK §4 hard rules.
 */

export * from './types.js';
