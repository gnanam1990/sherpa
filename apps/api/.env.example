# apps/api environment

# Paymaster RPC is sensitive and must stay server-side. M3 should validate
# blank values with z.preprocess(emptyToUndefined, z.string().url().optional()).
SHERPA_PAYMASTER_RPC=
