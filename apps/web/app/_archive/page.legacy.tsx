/* Legacy home page — preserved for rollback during
   Glass Aurora migration. Safe to delete after Phase 5
   verification complete. */

import { HomeContent } from './_components/HomeContent';

export default function HomePage() {
  return <HomeContent />;
}
