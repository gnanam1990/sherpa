# Stage 2 P6: Mainnet Flip

**Date:** 2026-05-15

## What was built
- Chain configuration via SHERPA_CHAIN env var
- Mainnet token addresses (USDC, WETH, USDT, cbBTC)
- Mainnet protocol addresses (Aerodrome, Aave)
- Startup validation for mainnet config completeness
- Fail-closed simulation on mainnet
- Chain-specific address resolution

## Key files changed
- packages/config/src/index.ts (ChainEnvSchema, mainnet validation)
- packages/tools/src/registry.ts (mainnet token addresses)
- packages/tools/src/aerodrome/index.ts (mainnet router)
- packages/tools/src/aave/index.ts (mainnet pool)
- packages/agentkit/src/planner.ts (fail-closed on mainnet)
- packages/safety/src/rings.ts (mainnet safety checks)
- apps/api/src/server.ts (startup validation)

## Testing
- Config tests: chain derivation, fail-open/closed defaults
- Registry tests: chain-specific token resolution
- Safety tests: mainnet config validation

## Notes
- This PR doesn't flip the chain — it enables the flip via env var
- Actual flip: set SHERPA_CHAIN=base-mainnet in Railway + Vercel
- Monitor for 7 days post-flip
