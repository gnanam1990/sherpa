# Stage 2 P5: 0.1% Protocol Fee

**Date:** 2026-05-15

## What was built
- Fee configuration via env vars (treasury, enabled, bps)
- Fee-aware swap builder (ERC-20 transfer to treasury)
- Fee validation in safety rings
- Fee fields on SwapPlan type
- Tests for fee computation, builder, and config

## Key files changed
- packages/config/src/index.ts (FeeEnvSchema, feeTreasuryAddress/feeEnabled/feeBps on SherpaConfig)
- packages/tools/src/aerodrome/swap-builder.ts (buildSwapWithFee — feeBps, treasuryAddress args)
- packages/tools/src/aerodrome/types.ts (SwapFeeConfig type)
- packages/safety/src/rings.ts (validateFeeTransfer)
- packages/safety/src/allowlist.ts (registerTreasuryAddress)
- packages/tools/src/aerodrome/swap-builder.test.ts (5 new fee tests)
- packages/config/src/index.test.ts (4 new fee config tests)

## Testing
- Swap builder fee tests: 5 tests (with fee enabled, without fee, fee computation, treasury encoding, zero bps)
- Config parsing tests: 4 tests (defaults, enabled, bps, treasury address)
- Safety ring fee validation tests: 3 tests (existing)

## Notes
- Fee disabled on Sepolia by default
- Cross-domain: M2 needs to surface protocolFeeAmount in ConfirmationCard
